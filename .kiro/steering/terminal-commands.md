---
inclusion: always
---

# Terminal commands

On this Windows workspace, use `cmd`-compatible commands for terminal operations. Do not use the `execute_pwsh` tool; prefer a cmd-based terminal tool when one is available.
